import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
   formWrapper:{
    margin:"5rem auto"
   },
   button:{
      marginTop:"1rem",
   },
   date:{
      marginTop:".9rem"
   }
}));